@model Keas.Mvc.Models.ReportModels.AdminHistoryReportViewModel

<form asp-action="ActionsByAdmins" method="get">
    <div class="card people-color">
        <div class="card-header-people">
            <div class="card-head">
                <h2>Lets Spy for ISO</h2>
            </div>
        </div>

        <div class="card-content">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="row align-items-end">
                <div class="col-sm">
                    <h3>Filters</h3>
                    <div class="form-group">
                        <label asp-for="Start" class="control-label"></label>
                        <input asp-for="Start" class="datepicker datepicker-input" type="text" data-date-orientation="bottom" data-date-clear-btn="true" data-date-today-highlight="true">
                        <label asp-for="End" class="control-label"></label>
                        <input asp-for="End" class="datepicker datepicker-input" type="text" data-date-orientation="bottom" data-date-clear-btn="true" data-date-today-highlight="true">
                        <label asp-for="PersonId" class="control-label"></label>
                        <select asp-for="PersonId" class="form-control" asp-items="@(new SelectList(Model.Admins, "Id", "Name"))"></select>
                        <div class="form-group">
                            <i class="fas fa-sync-alt fa-sm"></i><input type="submit" value="Refresh Report" class="btn btn-link" />
                        </div>
                    </div>
                </div>
                <div class="col-sm">
                </div>
            </div>

            @*<table id="dtDirectReports" class="table dataTable">
                <thead>
                    <tr>
                        <th>
                            Name
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Reports != null)
                    {
                        foreach (var item in Model.Reports)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(a=> item.Name)
                                </td>
                                <td>
                                    <a href='@string.Format("/{0}/{1}/details/{2}", TempData["TeamName"], "People", item.Id)'>Details</a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>*@

        </div>
    </div>
</form>
