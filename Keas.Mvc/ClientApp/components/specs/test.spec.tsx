import { mount, render, shallow } from 'enzyme';
import * as React from 'react';

describe('fake component', () => {
  it('should rreturn true', () => {
    const b = true;

    expect(b).toBe(true);
  });
});
