extraction:
  csharp:
    prepare:
      packages: "nodejs"
    index:
      dotnet:
        arguments: "--configuration Release"
        version: "3.1.301"
path_classifiers:
  library:
    - "**/wwwroot/lib/*"
